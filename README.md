# Incident Response Playbook

This repository holds the platform team's on-call incident-response checklist.

The checklist is maintained under version control so that every change is reviewed before it lands on `main`. Keep the content current as our incident processes evolve, and route updates through pull requests so the whole rotation has a chance to sign off.
